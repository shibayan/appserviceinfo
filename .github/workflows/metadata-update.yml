name: Update metadata

on:
  schedule:
    - cron: 0 */6 * * *
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    environment: production
    strategy:
      fail-fast: false
      matrix:
        location: [japaneast, westus2, eastus2, centralus, eastasia, southeastasia, northeurope, westeurope, koreacentral, australiaeast, francecentral, uksouth, canadacentral, qatarcentral, centralindia, uaenorth, eastus, westus3, southcentralus, brazilsouth, norwayeast, switzerlandnorth]
    permissions:
      id-token: write
      contents: read
    steps:
    - name: Azure Login
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: Download metadata
      run: |
        curl -f -o platform.json https://app-graffias-${{ matrix.location }}.azurewebsites.net/api/platform
        curl -f -o runtime.json https://app-graffias-${{ matrix.location }}.azurewebsites.net/api/runtime
        curl -f -o siteextension.json https://app-graffias-${{ matrix.location }}.azurewebsites.net/api/siteextension

    - name: Gzip download metadata
      run: |
        gzip --best platform.json
        gzip --best runtime.json
        gzip --best siteextension.json

    - name: Upload metadata to Blob
      run: |
        az storage blob upload --auth-mode login -f platform.json.gz --account-name stgraffias -c metadata -n ${{ matrix.location }}/platform.json --content-encoding gzip --content-type application/json --overwrite
        az storage blob upload --auth-mode login -f runtime.json.gz --account-name stgraffias -c metadata -n ${{ matrix.location }}/runtime.json --content-encoding gzip --content-type application/json --overwrite
        az storage blob upload --auth-mode login -f siteextension.json.gz --account-name stgraffias -c metadata -n ${{ matrix.location }}/siteextension.json --content-encoding gzip --content-type application/json --overwrite
